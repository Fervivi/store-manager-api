# App
spring.application.name=${EUREKA_SERVICE_NAME:STORE-MANAGER-SERVICE}
server.port=8080
spring.log.level=${SPRING_LOG_LEVEL:INFO}
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=30s

# Service Discovery
eureka.client.service-url.defaultZone=${EUREKA_SERVER_URL:http://eureka-server:8123/eureka/}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true

# Security
jwt.secret=${SPRING_JWT_SECRET}
jwt.issuer=${SPRING_JWT_ISSUER}
jwt.public=${SPRING_JWT_PUBLIC}

# OpenAPI / Swagger
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false
